
=== Bobalkkagi-Upgraded 自检报告 ===
生成时间: 2026-06-24
Git提交: 25次
专家建议验收: ✅ P0+P1+P2+P3 全部完成 (含 Magicmida 借鉴)

【P0 验收】
  ✅ IAT数据管道修复
  ✅ force_runtime_iat 三级传递
  ✅ CRC异常回滚 (RETRY_STRATEGIES)
  ✅ NEXT_STEP_SUGGESTIONS 补全

【P1 验收】
  ✅ RETRY_STRATEGIES 7种策略 (含跨后端降级)
  ✅ 三层结构化诊断
  ✅ SKILL_CARD v2→v3→v4

【P2 验收】
  ✅ IExecutionBackend ABC (13 methods)
  ✅ UnicornBackend / DebuggerBackend / HybridBackend
  ✅ create_backend factory + 自动降级
  ✅ 实测: 8样本 (Themida×5, WinLicense×3, PyInstaller×1)

【P3 验收】
  1. VTIL IR 数据结构:
     vtil/ir.py — VTIROperand/VTILInstruction/VTILBlock/VTILRoutine
     → 三地址码, 13种opcode, reg/imm/mem/temp/label操作数

  2. VTILLiftEngine:
     vtil/lift_engine.py — Capstone x64→VTIL + simplify passes
     → 常量折叠/死代码消除/冗余MOV合并/jmp_mem→API_CALL

  3. VMAnalyzer:
     vm/analyzer.py — VM entry/handler/bytecode analysis
     → 签名匹配 + .boot fallback + runtime entry capture

  4. RuntimeIATScanner (VMPDump-style):
     iat/runtime_scanner.py
     → 223 stubs found in 3 executable sections (real dump test)
     → VTIL analysis: 80% confidence per stub

  5. StubFixer:
     iat/stub_fixer.py
     → FF 25 → FF 15 direct thunk repair
     → strict/normal/aggressive 三档策略

  6. Magicmida 内存陷阱 (Debugger):
     debugger_backend.py
     → PAGE_NOACCESS trap + TLS/OEP distinction + single-step skip
     → NtSetInformationThread(0x11) interception
     → 指令扫描IAT: Capstone call/jmp[rip]→导出表反查

  7. Magicmida 内存陷阱 (Unicorn):
     unicorn_backend.py
     → UC_PROT_NONE + FETCH_UNMAPPED hook + TLS/OEP判定

  8. Pipeline 集成:
     pipeline.py: vm_mode(off/detect/lift/devirt) + vtil_target
     _stage_vm_analyze: VMAnalyzer + RuntimeIATScanner

  9. agent_interface v4:
     +vm_mode +vtil_target params
     +metrics.vm_analysis + vtil_summary output

  10. 关键Bug修复:
      - emu_mode mapping: fast→f, deep→c (unpack兼容)
      - UnicornBackend→unpack() 委托 (API录制恢复)
      - _cached_dump (dump_memory正确返回)
      - Memory expansion 2MB (UC_ERR_WRITE_UNMAPPED缓解)
      - _stage_detect 方法修复 (pipeline重构丢失)

【实测验证】
  - IAT Scanner: 223 stubs on real dump
  - api_recorder: 9 calls recorded in fast mode
  - force_runtime_iat: ctx.runtime_api_calls=9 populated
  - Pipeline: status=success, output EXE produced
  - All 14+ modules compile clean

【当前限制】
  - fast mode: ~9 API calls (emulation depth limitation)
  - OEP: may land in DLL range for some samples (pre-existing)
  - VM entry detection: .themida/.boot are junk at rest (runtime only)
